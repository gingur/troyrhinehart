// v1 scaffold placeholders. Real contact details and copy land in a later spec.
// NOTE: email/links are intentionally placeholders — do not commit personal PII here.
// The production email is plumbed separately (later spec); keep this file public-safe.
export const site = {
  name: 'Troy Rhinehart',
  tagline: 'Placeholder tagline — real copy ships in a later spec.',
  email: 'you@example.com',
  links: {
    linkedin: 'https://www.linkedin.com/in/your-handle',
    github: 'https://github.com/your-handle',
  },
} as const;
