name: Deploy

on:
  push:
    branches: [main]

permissions:
  id-token: write # required for GitHub OIDC
  contents: read

jobs:
  deploy:
    uses: gingur/devkit/.github/workflows/deploy-cf-worker.yml@main
    with:
      environment: production
      infisical-project-slug: gingur-7xjq
      infisical-env-slug: production
      infisical-secret-path: /troyrhinehart
      infisical-identity-id: f87b7cde-3016-4e85-84b5-0d077e8833e0
